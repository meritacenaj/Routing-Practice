import { Link } from "react-router-dom";

const Home = (props) => {

    const {results} = props;
    return (
        <div>
            <h1 style={{ color: "red" }}>Welcome</h1>
        </div>

    );
}
export default Home;